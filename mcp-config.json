{
  "_comment": "KEPTA — MCP config for Claude Desktop. Copy this file to ~/Library/Application Support/Claude/claude_desktop_config.json (macOS), or leave it here as mcp-config.json. Adjust 'cwd' and the path to match your checkout.",
  "mcpServers": {
    "kepta": {
      "command": "npx",
      "args": [
        "tsx",
        "src/mcp-server.ts"
      ],
      "cwd": "/ABSOLUTER/PFAD/ZU/kepta",
      "env": {}
    },
    "kepta-built": {
      "_comment": "Alternative nach 'npm run build:mcp' — ohne tsx, direkt Node auf gebuildettem CJS",
      "command": "node",
      "args": [
        "/ABSOLUTER/PFAD/ZU/kepta/dist/mcp-server.cjs"
      ],
      "env": {}
    }
  }
}