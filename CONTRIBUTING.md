# Contributing

1. Fork + Branch (`feat/...`)
2. `npm install && npm run lint && npm run build` muss grün sein
3. PR mit Beschreibung + Screenshot bei UI-Änderungen

Kein Geheimnis committen. Kein `KI-Gehirn` Branding mehr — nutze `KEPTA`.
