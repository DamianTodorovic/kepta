{
  "_comment": "KEPTA — mcp.json (Cursor / Windsurf / Zed). Lege diese Datei ins Projekt-Root oder .cursor/mcp.json. Siehe auch mcp-config.json für Claude Desktop.",
  "mcpServers": {
    "kepta": {
      "command": "npx",
      "args": ["tsx", "src/mcp-server.ts"],
      "env": {}
    }
  }
}
