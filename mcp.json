{
  "_comment": "KEPTA — mcp.json (Cursor / Windsurf / Zed). Put this file in the project root, or at .cursor/mcp.json. See mcp-config.json for Claude Desktop and for the alternatives.",
  "mcpServers": {
    "kepta": {
      "command": "npx",
      "args": ["-y", "kepta"],
      "env": {}
    }
  }
}
